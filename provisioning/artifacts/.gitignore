# Generated by scripts/upgrade-periphery.sh and served mesh-only at /artifacts/<name>.
# Not source: the installer is fetched from upstream and verified against
# bootstrap/periphery/versions.env on every publish, so committing it would just be a stale copy.
*
!.gitignore
